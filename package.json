{
  "name": "multisig-wallet",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test:hardhat": "hardhat test",
    "test:foundry": "forge test",
    "test": "pnpm test:hardhat && pnpm test:foundry",
    "lint": "solhint contracts/**/*.sol",
    "format": "prettier --write .",
    "coverage": "echo 'Coverage to be configured later if needed'",
    "deploy:local": "echo 'Add local deploy script later'",
    "deploy:testnet": "echo 'Add testnet deploy script later'",
    "deploy:mainnet": "echo 'Add mainnet deploy script later'",
    "security:slither": "slither .",
    "security:mythril": "myth analyze ./contracts/src/core --solc-json solc_input.json",
    "clean": "hardhat clean && forge clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "2.0.12",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "@openzeppelin/contracts": "^5.0.1",
    "@typechain/hardhat": "^9.1.0",
    "ethers": "^6.13.5",
    "hardhat": "^2.22.18",
    "prettier": "^3.4.2",
    "prettier-plugin-solidity": "^1.4.2",
    "solhint": "^5.0.5"
  }
}